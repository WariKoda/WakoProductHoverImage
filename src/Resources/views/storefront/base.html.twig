{% sw_extends '@Storefront/storefront/base.html.twig' %}

{% block base_body_classes %}
    {{ parent() }}
    {% if config('WakoProductHoverImage.config.enabled') is same as(true) %} is-wako-product-hover-image-enabled{% endif %}
{% endblock %}
